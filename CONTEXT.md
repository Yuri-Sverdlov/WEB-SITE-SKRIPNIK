# CONTEXT — память проекта WEB-SITE-SKRIPNIK

> Обновляет **архитектор** при смене фокуса. Кодер читает при старте сессии (см. AGENTS.md).

**Обновлено:** 2026-09-20 (этап B — раздел «Рассказы»)

## Суть

Сайт-портфолио для автора рассказов: витрина текстов, обратная связь читателей (комментарии, гостевая книга). Пилотный проект; после запуска — ещё 1–2 аналогичных сайта на той же архитектуре.

Полное ТЗ: **`ТЗ — сайт для писателя.md`**

## Пути и репозиторий

| | |
|---|---|
| Локальная папка (этот ПК) | `F:\_MY_PROGRAMMING_3\WEB-SITE-SKRIPNIK` |
| GitHub | https://github.com/Yuri-Sverdlov/WEB-SITE-SKRIPNIK |
| Стандарты среды | `F:\DEV-NOTES.md` |

## Стек (план)

| Слой | Технология |
|---|---|
| Фронтенд | React + Vite + TypeScript + Tailwind CSS |
| Бэкенд | Уточняется (Node.js/Express или BaaS, в этапах — Supabase) |
| Проверка UI | Playwright (скриншоты vs макет) |
| Деплой (этап 0) | Vercel |

## Участники

| Участник | Роль |
|---|---|
| **Пользователь (Yuri)** | Продуктовые решения, утверждение этапов, связь между консультантом и архитектором |
| **Консультант (Claude Desktop)** | Этапы и вехи, свежий взгляд, помощь при серьёзных проблемах; **не** заменяет архитектора и кодера |
| **Архитектор (Cursor, чат)** | Проектирование, `tasks/TASK.md`, проверка, журнал; код почти не пишет |
| **Кодер (терминал: DeepSeek / Ollama / Claude Code)** | Реализация по TASK, отчёт в `tasks/REPORT.md`, commit/push по заданию |

## Канонические файлы процесса

1. `AGENTS.md` — устав кодера
2. `CONTEXT.md` — этот файл
3. `tasks/TASK.md` — одно активное задание
4. `tasks/REPORT.md` — один текущий отчёт
5. `PROJECT_LOG.md` — журнал сессий (append-only)
6. `tasks/done/` — архив принятых TASK + REPORT + ACCEPTED

«Правда о состоянии» — только в `REPORT.md` (сейчас) и `PROJECT_LOG.md` (история).

## Текущий фокус

**Этап B** — полностью рабочий раздел «Рассказы» (консультантский бриф).

**Статус:** **Этап B завершён и принят** (2026-09-20). Раздел «Рассказы» на Supabase рабочий. Следующий этап — ждём от консультанта.

**Решения (зафиксировано):**
- Счётчик: RPC `increment_story_views(uuid)` — не прямой UPDATE
- Роуты `/stories/new|popular|all` + общий `StoriesTabNav`
- Поиск (TASK-003): один общий, по всей таблице, игнорирует фильтр вкладки
- Пагинация 15/стр. на всех вкладках
- `illustrations`: `text[]` URL; пусто — блок не показывать

## Этапы (кратко, из ТЗ §9)

| # | Этап |
|---|---|
| 0 | Инициализация (конвейер) |
| 1 | Каркас страниц и навигация |
| 2 | Supabase: схема и подключение |
| 3 | Список рассказов (моки, вкладки, пагинация) |
| 4 | Страница рассказа |
| 5 | Регистрация читателей |
| 6 | Комментарии |
| 7 | Гостевая книга |
| 8 | Backend-редактор автора |
| 9 | Защита от спама |
| 10 | Визуальная доводка + домен |

После MVP: перенос 200+ рассказов с Проза.ру (отдельный этап, не в MVP).

## Открытые вопросы (решить до/в ходе разработки)

- Регистрация: email+пароль vs OAuth (Google/Яндекс)
- Хостинг/домен
- Макет главной (ожидается от пользователя)
- Бэкенд: свой сервер vs BaaS (в этапах заложен Supabase)

## Вне MVP (MVP-2)

Адаптив, SEO, архив по датам, шаринг, RSS, тёмная тема, WYSIWYG-редактор и др. — см. ТЗ §8.

## Структура кода (ориентир из ТЗ)

```
src/
  components/
  pages/
  api/
reference/
  desktop.png
```
