# Задание (архитектор → кодер)

**ID:** TASK-003  
**Дата:** 2026-09-20  
**Статус:** активно  
**Этап B (часть 2/2):** StoryDetail + RPC-счётчик + поиск

> TASK-002 принят архитектором 2026-09-20.

---

## Цель

Завершить этап B: **страница рассказа**, **инкремент просмотров**, **общий поиск** в разделе «Рассказы».

---

## Контекст

- RPC уже в Supabase (выполнил пользователь):
  ```typescript
  supabase.rpc('increment_story_views', { story_id_input: id })
  ```
  **Не** делать прямой `UPDATE` с фронта.
- Поиск: одна строка в `StoriesTabNav`, при непустом запросе — **плоский список по всей таблице** (игнорирует фильтр вкладки Новые/Популярные/Все).
- Поиск: `.or(\`title.ilike.%${q}%,content.ilike.%${q}%\`)` — экранируй `%` и `_` в пользовательском вводе или используй безопасный паттерн.
- `illustrations`: `text[]` URL; если пусто — блок не показывать.
- Тест: «Разговор с отцом» — поиск по слову из title/content должен находить.

---

## Что сделать

### 1. `src/pages/StoryDetail.tsx`

- Загрузка по `useParams().id`: `select('*').eq('id', id).single()`
- Показать: `title`, `content` (с сохранением абзацев — `whitespace-pre-wrap`), `tags`, `published_at`, `views_count`
- `illustrations`: если массив непустой — `<img>` для каждого URL
- При **первой загрузке** страницы вызвать RPC `increment_story_views`, затем обновить отображаемый счётчик (+1 локально или refetch)
- Состояния: загрузка, «рассказ не найден», ошибка
- Ссылка «← к списку» на `/stories/all` (или таб-nav)

### 2. Поиск — доработать `StoriesTabNav` + списки

**Вариант архитектора (реши сам, опиши в REPORT):**

- Добавить в `StoriesTabNav` controlled input поиска
- Поисковый запрос хранить в **URL query** `?q=...` (чтобы работал на всех `/stories/*`) — рекомендуется
- Когда `q` непустой: `StoryListPage` (или обёртка) вызывает `searchStories(page, q)` вместо `fetchStoriesNew/Popular/All`
- Новая функция в `src/api/stories.ts`:
  ```typescript
  searchStories(page: number, query: string): Promise<StoriesPageResult>
  ```
  с `.or(...ilike...)` + `.order('published_at', { ascending: false })` + `.range()`
- При активном поиске — заголовок «Результаты поиска» (или аналог), табы остаются видимыми

### 3. Scope — не трогать

- Глобальный `App.tsx` nav
- SQL / Supabase Dashboard
- Комментарии, auth, гостевая книга

---

## Проверки (полный вывод в REPORT)

### A. `npm run build` — exit 0

### B. StoryDetail

1. Открыть рассказ по ссылке из списка
2. Видны title, content, tags, дата, views_count
3. **Счётчик:** запомнить число → F5 дважды → число **выросло на 2** (MVP: без dedup)
4. Консоль без ошибок

### C. Поиск

1. На любой вкладке ввести слово из названия (напр. «снег») — находится «Первый снег»
2. Поиск «отец» — находится «Разговор с отцом»
3. Пустой запрос — возврат к обычному списку текущей вкладки

### D. Sanity в REPORT

- Начальный и финальный `views_count` после двух refresh (скрин или числа)
- SQL/RPC ошибок нет

---

## Git

**Не делать** commit/push.

---

## Критерии приёмки

- [ ] StoryDetail полный, RPC increment работает
- [ ] Поиск по title/content, общий для раздела
- [ ] illustrations скрыты если пусто
- [ ] REPORT с полным выводом

---

## Отчёт

Заполни **`tasks/REPORT.md`**.
